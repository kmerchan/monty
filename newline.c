#include "monty.h"

/**
 * newline_func - function to catch newline or empty line set to newline
 * @stack: input pointer to head of stack
 * @line_number: line count currently reading instructions from
 */

void newline_func(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
